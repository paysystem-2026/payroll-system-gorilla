import { RefreshCw } from "lucide-react";
import { PlaceholderPage } from "@/components/common/PlaceholderPage";

export function UpdatesPage() {
  return (
    <PlaceholderPage
      title="Updates"
      description="Update checking, downloading, verification, and rollback"
      icon={RefreshCw}
    />
  );
}
